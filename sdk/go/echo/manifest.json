{
  "id": "echo",
  "version": "0.1.0",
  "kind": "service",
  "display_name": "Echo (SDK example)",
  "description": "Minimum agent sample: takes text input, returns text output. No host capability dipakai. Pakai sebagai template untuk agent baru.",

  "min_kernel_version": "0.1.0",
  "abi_version": 1,

  "author": "@flowork-os",
  "license": "MIT",

  "entry": "agent.wasm",
  "memory_max_mb": 4,

  "capabilities_required": [],

  "exposes_rpc": [
    {
      "name": "echo",
      "description": "Return the text the caller sent, prefixed with a server timestamp.",
      "input_schema": {
        "type": "object",
        "properties": { "text": { "type": "string" } },
        "required": ["text"]
      },
      "output_schema": {
        "type": "object",
        "properties": { "reply": { "type": "string" } }
      }
    }
  ]
}
