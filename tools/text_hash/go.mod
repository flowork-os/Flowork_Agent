// Modul SENDIRI per-tool (isolasi). Dependency tool ada di go.mod + vendor FOLDER INI —
// NOL shared lib (itu yang bikin plug-and-play + agnostic). Build pakai GOWORK=off.
module flowork-tool-text-hash

go 1.21
